{
  "name": "@repo/scripts",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "db:clean:users": "tsx ./dbCleanUsers.ts",
    "db:seed:users": "tsx ./dbSeedUsers.ts",
    "lint-staged": "lint-staged"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "drizzle-orm": "^0.44.6"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "tsx": "^4.20.6",
    "lint-staged": "^17.0.7"
  },
  "lint-staged": {
    "**/*.{ts,tsx}": [
      "pnpm exec eslint --fix",
      "pnpm exec prettier --write"
    ],
    "**/*.{md,json}": [
      "pnpm exec prettier --write"
    ]
  }
}
