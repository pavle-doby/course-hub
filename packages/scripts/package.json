{
  "name": "@repo/scripts",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "db:clean:users": "tsx ./dbCleanUsers.ts",
    "db:seed:users": "tsx ./dbSeedUsers.ts",
    "db:seed:course-content": "tsx ./dbSeedCourseContent.ts",
    "db:clean:course-content": "tsx ./dbCleanCourseContent.ts",
    "lint-staged": "lint-staged"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "lint-staged": "^17.3.0",
    "tsx": "^4.23.12"
  },
  "lint-staged": {
    "**/*.{ts,tsx}": [
      "pnpm exec eslint --fix",
      "pnpm exec prettier --write"
    ],
    "**/*.{md,json}": [
      "pnpm exec prettier --write"
    ]
  }
}
